export type RepositoryFileItem = {
    id: number | null,
    analyzed: boolean | null,
    language: string | null,
    repo_relative_file_path: string | null,
    metrics: string | null,
    analyzed_time: number | null,
    repository_id: number | null
}